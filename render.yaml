services:
  - type: web
    name: auto-reply-api
    env: node
    region: singapore  # or your preferred region
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true